interface:
  display_name: "Commit Code"
  short_description: "Review local changes and create scoped commits after confirmation."
  default_prompt: "Use $commit-code to review my workspace changes and commit them in clean, scoped batches."
